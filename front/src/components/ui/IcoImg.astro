---
interface Props {
    src: string;
    alt: string;
    class?: string;
}

const { src, alt, class: className = 'size-4' } = Astro.props;
---

<figure>

    <img
        src={src}
        alt={alt}
        class={className}
    />
</figure>